apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      run: frontend
  template:
    metadata:
      labels:
        run: frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/lilpernil/go-app-ui:1.0-b49f87bd
        env:
          - name: BACKEND_DNS
            value: backend-svc
          - name: BACKEND_PORT
            value: '9000'